      - name: Run Wake detectors (non-blocking)
        run: |
          wake detect all || true

      - name: Upload Wake outputs
        uses: actions/upload-artifact@v4
        with:
          name: wake-output
          path: .wake/
          if-no-files-found: warn
